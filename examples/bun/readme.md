# Bun Example

The simplest Solarflare app using Bun — a single route with server handler and client component.

## Structure

```
src/
├── index.ts           # Worker handler
├── index.server.tsx   # Server handler
└── index.client.tsx   # Client component
```

## Quick Start

```sh
bun install
bun run build
bun start
```
