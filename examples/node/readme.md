# Node Example

The simplest Solarflare app using `node:http` instead of cloudflare — a single route with server handler and client component.

## Structure

```
src/
├── index.ts           # Worker handler
├── index.server.tsx   # Server handler
└── index.client.tsx   # Client component
```

## Quick Start

```sh
npm install
npm start
```
